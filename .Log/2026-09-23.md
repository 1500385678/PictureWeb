# 2026-09-23 PictureWeb v2.1.7 → v2.1.8

## 一天内完成的事

### 数据库迁到 `G:\_MyDatabase\01_Space_ImageDb`

- 旧的 `D:\Database\Database\05_Space\01_ImageDB\` 6 个 db + 老顶层
  SpaceDb.db 总表全表路径修:
  `01_SpaceDb` → `01_Space_ImageDb`(770 条 abs_path)
- 6 个分库 metadata 同步(755 行,0 missing)
- 6 个分库 abs_path 同步(同名子目录路径跟着改)
- DiagramDb 11 张 `Style` 占位清空 + SpaceDb 总表改成
  `Diagram-(待补)` 占位(等 user 手动补)
- 4 个幽灵行删除(深圳后海 2 张图 × SpaceDb + MasterDb)
- 14 张嘉佰道·南京 webp 入库(SpaceDb + ResidenceDb)
  — **重复入库**,等 user 回滚

### 8 大踩坑教训(memory)

- `python sqlite3.execute()` 默认 deferred,必须显式 `db.commit()`
- server.py `_scan_dbs()` 启动时一次性扫描,改 server 必须重启
- `db_name` 派生:统一文件名,不再认父目录
- 浏览器看到"老数据"通常是 HTML/JS 缓存,Ctrl+F5
- 项目 0 个生成 thumbs 的代码 — `thumbs/` 嵌套是外部脚本的 bug

### 6 个 DB 自动化脚本(新增,全部本地试跑)

| 脚本 | 行数 | 用途 |
|---|---|---|
| `scripts/sync_split_dbs.py` | 159 | 总库→分库 12 个 metadata 列同步 |
| `scripts/clean_placeholder.py` | 146 | 清 Style/TBD/Diagram-(待补) 占位 |
| `scripts/verify_db.py` | 137 | 健康检查(metadata/abs_path/占位/备份) |
| `scripts/prune_missing.py` | 146 | 删 abs_path 不存在的幽灵行 |
| `scripts/ingest_directory.py` | 238 | 从目录入库,自动分类,phash 去重 |
| `scripts/phash_check_dups.py` | 127 | phash 查重工具 |

### AGENTS.md 双文件范式建立

- `PictureWeb/AGENTS.md`(§ 10 数据库规则,8 子节,接手必读)
- `G:\_MyDatabase\01_Space_ImageDb\AGENTS.md`(379 行,数据库专属)

### UI 工具栏优化

- `.search-box select` 92px(7 个分类下拉)
- `.search-box input#q` 220px(主搜索框)
- **db-select 移到主搜索栏最左**(2026-09-23)
  - 之前 db-select 在 `.keywords-box` 内,被 `overflow:hidden` 裁掉 kw-input
  - 现在:row1 = db-select → db-count → q → 7 selects → 3 buttons
  - row2 = 多关键词 → kw-input(独立,不挤)

### server.py 改动

- `PICTUREWEB_DEFAULT_DB` env override(优先级 env > PictureDb > DB_LIST[0])
- `_scan_dbs()` db_name 派生:`fn.lstrip('_')`(去 `.db` + 去下划线)
- 过滤 `.pre- / .bak. / .backup.` 备份文件(39 个文件不再当库列出)

### start_detached.py 改动(别人 session 加的)

- hot reload — server.py / __version__.py mtime 变就自动 kill + 重启
- /admin/restart endpoint(让 server 自己 0.5s 后退出,watchdog 拉起)

## 数据快照(2026-09-23 18:00)

| db | 行数 | 状态 |
|---|---|---|
| SpaceDb(总) | 782 | 待回滚 14 行嘉佰道 webp |
| MasterDb | 166 | 切库同步 0 missing |
| WeWorkDb | 25 | 切库同步 0 missing |
| ResidenceDb | 51 | 待回滚 14 行 + 切库同步 0 missing |
| BlockDb | 67 | 切库同步 0 missing |
| VisualizationDb | 447 | 切库同步 0 missing |
| DiagramDb | 11 | Style 清空 + Diagram-(待补) 占位 |

## 欠账(下次 session 接着做)

- 14 张嘉佰道 webp 重复入库的回滚(我已推荐方案 1)
- 39 个 `*.db.pre-fix-*` / `*.db.bak.*` 备份清理
- 11 个 `Diagram-(待补)` 占位在 SpaceDb 总表 + 11 个 Style 公司
  在 DiagramDb(verify_db.py 已 dry-run 显示会清 22 行)
- 入库去重要改用 phash 而不是 MD5(file_hash) — SpaceDb 没 file_hash 列
- 后台跑 `python scripts/verify_db.py` 周报(原则 11.1,重复 ≥ 3 必自动化)

## 当日 commits(预计)

- `feat: db_select 在主搜索栏最左 + 删冗余 width:240 CSS` (UI)
- `feat: 6 个 DB 自动化脚本(同步/清理/验证/剪枝/入库/查重)`
- `feat: env PICTUREWEB_DEFAULT_DB override + _scan_dbs db_name 改名`
- `chore: gitignore 补 .scratch/ + *.out`
- `docs: AGENTS.md §10 数据库规则(2026-09-23 总结 · 接手必读)`
- `release: v2.1.8(数据库迁 G + UI 优化 + 6 个 DB 工具)`
